# Windows环境专用依赖文件
# 包含所有功能模块的完整依赖列表

# ============ 核心Web框架 ============
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# ============ 数据库 ============
sqlalchemy>=2.0.0
alembic>=1.10.0

# ============ 认证和安全 ============
PyJWT>=2.6.0
python-jose[cryptography]>=3.2.0
passlib[bcrypt]>=1.7.0
python-multipart>=0.0.5

# ============ 配置和环境 ============
python-dotenv>=0.19.0
pydantic>=2.0.0
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
email-validator>=1.3.0

# ============ 文件和网络处理 ============
aiofiles>=0.8.0
httpx>=0.24.0
chardet>=5.0.0

# ============ 测试框架 ============
pytest>=7.0.0
pytest-asyncio>=0.20.0

# ============ 文档解析库 ============
pandas>=1.5.0
PyPDF2>=3.0.0
python-docx>=0.8.11
openpyxl>=3.0.0
markdown>=3.4.0
pdfplumber>=0.7.0

# ============ 中文文本处理 ============
jieba>=0.42.1

# ============ 机器学习和数据分析 ============
scikit-learn>=1.2.0

# ============ 图像和PDF增强处理 ============
Pillow>=9.0.0
PyMuPDF>=1.22.0

# ============ 可选：高级功能包 ============
# opencv-python>=4.7.0        # 图像处理（较大，可选安装）
# pytesseract>=0.3.10         # OCR文字识别（需要Tesseract程序）
# pdf2image>=1.16.0           # PDF转图像（需要poppler）
# nltk>=3.8                   # 自然语言处理（较大，可选安装）
# spacy>=3.5.0                # 高级NLP（很大，按需安装）
# requests>=2.28.0            # HTTP请求库
# beautifulsoup4>=4.11.0      # HTML解析

# ============ Windows环境问题包 ============
# netifaces                   # 在Windows上编译困难，已有替代方案